name: Sunday Bin Final Reminder (21:00)

on:
  schedule:
    - cron: '0 20 * * 0'    # 21:00 BST (summer)
    - cron: '0 21 * * 0'    # 21:00 GMT (winter)
  workflow_dispatch:
    inputs:
      force:
        description: 'Bypass the 21:00 Europe/London time check'
        required: false
        default: 'false'

jobs:
  remind:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Time check (21:00-21:20 Europe/London Sunday)
        id: timecheck
        run: |
          if [[ "${{ github.event.inputs.force }}" == "true" ]]; then
            echo "Force flag — proceeding"
            echo "should_run=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          DOW=$(TZ=Europe/London date '+%u')
          HOUR=$(TZ=Europe/London date '+%H')
          MIN=$(TZ=Europe/London date '+%M')
          echo "Europe/London now: ${HOUR}:${MIN} day-of-week=${DOW}"
          if [[ "$DOW" == "7" ]] && [[ "$HOUR" == "21" ]] && [[ "$MIN" -le 20 ]]; then
            echo "✅ Inside 21:00-21:20 Sunday window — proceeding"
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "⏭️ Outside window — silently skipping (the other cron entry handles BST/GMT)"
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

      - uses: actions/checkout@v4
        if: steps.timecheck.outputs.should_run == 'true'
      - uses: actions/setup-python@v5
        if: steps.timecheck.outputs.should_run == 'true'
        with:
          python-version: '3.11'
      - name: Install deps
        if: steps.timecheck.outputs.should_run == 'true'
        run: pip install --quiet playwright requests && python -m playwright install --with-deps chromium
      - name: Run cyberpunk final reminder
        if: steps.timecheck.outputs.should_run == 'true'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:   ${{ secrets.TELEGRAM_CHAT_ID }}
          GMAIL_SENDER:       ${{ secrets.GMAIL_SENDER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          EMAIL_CEZARY:       ${{ secrets.EMAIL_CEZARY }}
          EMAIL_SUNIA:        ${{ secrets.EMAIL_SUNIA }}
        run: python scripts/run_reminder.py --style cyberpunk
      - name: Upload artifact
        if: steps.timecheck.outputs.should_run == 'true' && always()
        uses: actions/upload-artifact@v4
        with:
          name: bin-reminder-cyberpunk-png
          path: /tmp/bin_reminder.png
          if-no-files-found: ignore
